/*Crie uma função para inverter uma string. Por exemplo, se a entrada for "hello", a saída será "olleh".*/

func inversao(word: String) {
    let palavrs = String(word.reversed())
    print(palavrs)
}

let resultado = "hello"
inversao(word: resultado)